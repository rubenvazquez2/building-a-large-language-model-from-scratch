[project]
name = "ballmfs"
description = "Building a Large Language Model from Scratch - personal sandbox"
version = "0.1.0"

[build-system]
requires = ["flit_core"]
build-backend = "flit_core.buildapi"
